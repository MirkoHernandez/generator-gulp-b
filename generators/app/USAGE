Description:
    Creates a gulpfile (and a package.json file if required).

Example:
    yo gulp-b

    This will create:
        gulpfile.js
	package.json (if there is no package.json file)

    yo gulp-b:sass

    This will update:
        gulpfile.js with a sass task to compile scss files.
	package.json to include gulp-sass

