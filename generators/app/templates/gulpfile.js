'use strict';

// Node 
var del = require('del');

// Gulp plugins
var gulp = require('gulp');
var sass = require('gulp-sass');

// Paths
var paths = {
    index: {  
	src: './src/index.html',
	dest: './dist'
    },
};

gulp.task('default', function () {
    
});
